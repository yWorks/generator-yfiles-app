
initialize() {
  // create a GraphComponent
  var graphComponent:yfiles.view.GraphComponent = new yfiles.view.GraphComponent('graphComponent');

  // create and configure a default node style
  graphComponent.graph.nodeDefaults.style = new yfiles.styles.ShinyPlateNodeStyle({
    fill: yfiles.view.Fill.ORANGE,
    drawShadow: true
  });

  // get the IGraph
  var graph:yfiles.graph.IGraph = graphComponent.graph;
  // create some nodes
  var node1:yfiles.graph.INode = graph.createNode(new yfiles.geometry.Rect(50, 50, 30, 30));
  var node2:yfiles.graph.INode = graph.createNode();

  // create an edge
  graph.createEdge(node1, node2);

  // create an input mode
  graphComponent.inputMode = new yfiles.input.GraphEditorInputMode();
}

constructor() {
  this.initialize();
}
