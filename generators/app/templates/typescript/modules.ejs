<% if (useWebpack) { %>require("license");<% } %>
<% if (useWebpack || useBrowserify) {
    for (var i = 0; i < modules.length; i++) { %>
require("<%- (useBrowserify?'./':'') + modules[i] %>");<%
    } %>
require("<%- (useBrowserify?'./':'') + applicationName %>");<%
} else {
%>require(["yfiles", "<%- "scripts/" + applicationName + ".js" %>"]);<%
} %>
