<% moduleList.forEach(function(module, idx) { %>import <%= idx === 0 ? "yfiles" : "tmp" + idx%> = require("<%= module %>");
<%})%>

/**
 * <% if (description) { %><%= description %><% } else { %>A yfiles class.<% } %>
 */
export class <%- name %> {
  <% if (content) { %><%-content %><% } %>
}
<% if (postClassContent) { %><%- postClassContent %><% } %>

